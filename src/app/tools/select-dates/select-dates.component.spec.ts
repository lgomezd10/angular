import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SelecciondatesComponent } from './select-dates.component';

/*describe('SelecciondatesComponent', () => {
  let component: SelecciondatesComponent;
  let fixture: ComponentFixture<SelecciondatesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SelecciondatesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SelecciondatesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
